import 'package:flutter/material.dart';

class CenteredSirculerProgressIndecator extends StatelessWidget {
  const CenteredSirculerProgressIndecator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
